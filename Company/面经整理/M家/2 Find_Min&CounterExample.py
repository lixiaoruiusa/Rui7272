"""
2. find_min function is implemented wrongly (the function was implemented to always return 0), 
write a function to return a counter example array of size n that when passed to find_min will always return a wrong answer. 
E.g n = 4, [100, 500, 200, 300].

2. 给一个有bug的find_min function，然后让我们写一个function去返回一个int array，能够让那个find_min返回错的结果。
解：只要把最小的数（非0）放在那个array的第一位就好了

""""""
